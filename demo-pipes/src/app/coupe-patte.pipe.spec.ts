import { CoupePattePipe } from './coupe-patte.pipe';

describe('CoupePattePipe', () => {
  it('create an instance', () => {
    const pipe = new CoupePattePipe();
    expect(pipe).toBeTruthy();
  });
});
